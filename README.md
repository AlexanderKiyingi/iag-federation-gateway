# iag-federation-gateway

Central sync and conflict resolution for federated IAG deployments.

Reserved scaffold â€” implementation will follow platform patterns used by `iag-fleet` and `iag-procurement` (gateway auth, Kafka events, shared notifications).

Registry: [`subrepos.json`](../../subrepos.json) 
